name: Demo Workflow
on:
  push:
    branches:
      - master
jobs:
  print:
    runs-on: windows-latest
    steps:
      - name: Print to console
        run: echo Hello GH Actions!
      - name: Print a few lines
        run: echo First line
          echo Second line
